# 판단기준

도메인이 바뀌어도 유지되는 원칙만 적는다. 도메인·프로젝트별 구체적인 숫자는 각 문서(`CLAUDE.md` 등)에 따로 남긴다.

## 2026-09-04

[신뢰성/표시 원칙]

- 계산과 표시는 다른 결정이다.
- 못 믿으면 계산하지 않는다.
- 계산해 놓고 숨기는 것이 아니다.
- 사람은 본 숫자를 기억하므로 회색 숫자도 보여주지 않는다.
- 대신 감춘 이유는 자세히 적는다.
- 표본이 적으면 할 수 있는 것만 한다.
- 인과를 주장할 수 없는 비교는 인과 표현을 쓰지 않는다.
- 판정 순서를 코드로 고정한다.
- 쪼개는 축은 손을 쓸 수 있는 축으로 고른다.
- 색은 판정을 전달하며 장식에 사용하지 않는다.

이번 프로젝트(my-report, 내부회계관리제도 운영 및 개선관리)에서 실제 정한 내용:

- 분해 축: process_name
- 최소 표본: 20
- 관측기간 이슈: 2026 개선조치 22건 중 12건 target_date가 2027년

## 2026-09-04 (추가)

- 임계값이 정의된 KPI만 정상/경고/위험을 판정한다.
- 임계값이 아직 없는 KPI는 "참고지표"로 표시한다.
- 참고지표는 신뢰성 판정 보류(표본·관측기간 미충족)와 다른 개념이다 — 하나는 "기준이 없다", 다른 하나는 "믿을 수 없다".
- 임계값을 임의로 만들지 않는다.

## 2026-09-04 (이전 결정 보완 기록)

전수 점검에서 [데이터 검증 원칙]·[계산 원칙] 두 묶음이 빠져 있음을 확인했다. 새로운 결정이 아니라, 이미 코드(`core/validate.py`·`core/metrics.py`)에 있던 내용을 보편 원칙으로 옮겨 적는다.

[데이터 검증 원칙]

- 계산이 틀리게 되는 오류는 차단한다.
- 해석만 주의하면 되는 것은 경고로 둔다.
- 검증 규칙은 과도하게 늘리지 않는다.
- 임계값에는 근거가 필요하다.
- 메시지에는 실제 숫자를 포함한다.
- 구조적 결측은 임의로 채우지 않는다.
- 깨진 입력을 실제로 넣어 차단 여부를 확인한다(가능할 때).

[계산 원칙]

- 무엇을 하나로 셀지는 사람이 결정한다.
- 그레인·분모·필터·기간을 명시한다.
- 한 대상이 여러 행이면 팬아웃을 주의한다.
- 순차 단계가 아니면 퍼널이라 부르지 않는다.
- 관측기간이 다른 코호트를 성과 비교하지 않는다.
- 자동으로 채워진 가정을 찾아 명시한다.
