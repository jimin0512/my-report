# 판단기준

도메인이 바뀌어도 유지되는 원칙만 적는다. 도메인·프로젝트별 구체적인 숫자는 각 문서(`CLAUDE.md` 등)에 따로 남긴다.

## 적용

아래 부록(전체 판단기준)에 이미 있는 문장 중, 다른 데이터·다른 업무에도 그대로
적용할 수 있는 것만 그대로 옮긴다. 문장을 새로 만들거나 고치지 않는다.

- 표본이 적으면 할 수 있는 것만 한다. 최소 표본 기준을 두고 미달이면 판정을 보류한다(구체적 기준값은 프로젝트마다 정한다).
- 관측 데이터만으로는 인과를 단정하지 않는다.
- 본문 순서는 의미가 있으므로 임의로 바꾸지 않는다.
- 요청 문장에 결정 동사가 없으면 그것은 보고이지 제안이 아니다 — 최종 문서는 읽는 사람이 승인/조건부 승인/보류 중 하나를 판단할 수 있어야 한다.
- 값이 없으면 "미확인" 또는 todo로 정직하게 남긴다.

## 부록 — 전체 판단기준

### 2026-09-04

이 날짜 하루 동안 나온 원칙을 [데이터 검증]·[계산/그레인]·[신뢰성/표시]·[문장/책임] 4개 묶음으로 정리한다. 새 판단을 추가한 것이 아니라, 같은 날짜에 여러 차례 나눠 적었던 기록을 통합하고 중복을 하나로 합친 것이다.

[데이터 검증]

- 계산 자체를 틀리게 만드는 오류는 차단하고, 해석만 주의하면 되는 것은 경고로 둔다.
- 검증 규칙은 과도하게 늘리지 않는다. 임계값에는 근거가 필요하다.
- 경고·차단 메시지에는 실제 숫자를 포함한다.
- 구조적 결측은 임의로 채우지 않는다.
- 깨진 입력을 실제로 넣어 차단 여부를 확인한다(가능할 때).

[계산/그레인]

- 무엇을 하나로 셀지는 사람이 결정한다. 그레인·분모·필터·기간을 명시한다.
- 한 대상이 여러 행이면 팬아웃을 주의한다.
- 순차 단계가 아니면 퍼널이라 부르지 않는다.
- 관측기간이 다른 코호트를 단순 성과 비교하지 않는다.
- 자동으로(또는 조용히) 채워진 가정을 찾아 명시한다.

[신뢰성/표시]

- 계산과 표시는 다른 결정이다. 못 믿으면 계산 자체를 하지 않는다 — 계산해 놓고 화면에서 숨기는 구조가 아니다. 판정 순서를 코드로 고정한다.
- 표본이 적으면 할 수 있는 것만 한다. 최소 표본 기준을 두고 미달이면 판정을 보류한다(구체적 기준값은 프로젝트마다 정한다).
- 감춘 항목은 회색으로도 보여주지 않는다 — 사람은 본 숫자를 기억한다. 대신 감춘 이유·실제 조건값·기준값을 함께 적는다.
- 임계값이 없다는 것과 신뢰성 판정이 보류된 것은 다른 개념이다 — 하나는 "기준이 아직 없다"(참고지표), 다른 하나는 "표본·관측기간이 부족해 믿을 수 없다"(판정 보류). 같은 색·문구로 섞지 않는다.
- 쪼개는 축은 손을 쓸 수 있는 축으로 고른다.
- 색은 판정을 전달하며 장식에 사용하지 않는다.

[문장/책임]

- 결과가 틀렸을 때 사람이 책임지는 문장(배경·해석·제안처럼 판단이 들어가는 장)은 사람이 쓰고, 사실만 틀릴 수 있는 장은 자동으로 쓴다.
- 자동 문장과 사람 문장 모두 인과 표현 검사를 적용한다.
- 관측 데이터만으로는 인과를 단정하지 않는다.
- 검증에서 나온 경고와, 신뢰성 기준으로 판정을 보류한 항목은 모두 한계 절에 기록한다.
- 찾아봤지만 없었던 것도 결과로 기록한다 — "없음"도 결과다.
- 화면에서 감춘 숫자는 리포트·PDF·아카이브 어디에도 다시 쓰지 않는다.
- 되돌릴 수 없는 승인 단계는 그 사실을 화면에서 명확히 알린다.

### 현재 프로젝트 결정(my-report, 내부회계관리제도 운영 및 개선관리)

- 분해 축: process_name
- 그레인: Track B는 deficiency_id, 반복 미비점·개선 후 재발은 control_id 단위
- 유지·재발은 순차 퍼널이 아니라 판정 모집단을 나누는 결과 분류다
- 최소 표본: 20
- 공식 임계값은 metric-001(미해소 미비점 비율)만 있다. metric-002~004는 참고지표다
- 2026년 개선조치 22건 중 12건은 target_date가 2027년(관측기간 이슈)
- 사람 작성 장: 2. 배경 · 6. 해석 · 8. 제안
- 사람 작성 장의 작성 가이드라인은 화면 전용이다 — 본문·PDF·아카이브에 자동으로 들어가지 않는다

### 2026-09-09

오늘 9주차 발견 작업(신뢰성 재확인·분해축 점검·발견.md 작성) 중 나온 원칙과 결정을 정리한다. 기존 항목과 의미가 겹치는 것은 새 항목으로 추가하지 않고 이 절 끝에 어느 기존 항목과 같은지 남긴다.

[도메인과 무관한 원칙]

- 맞는 숫자와 근거로 쓸 수 있는 숫자는 다르다.
- 비율은 분자와 분모가 함께 있어야 한다.
- 비교 대상이 없으면 "낮다/높다"라고 판단하지 않는다.
- 비중이 없으면 그 문제가 전체에서 얼마나 큰지 판단하기 어렵다.
- 한 건이 바뀌어 결론이 뒤집힐 수 있는 수준이면 판단을 보류한다.
- 같은 비율이라도 분모 정의가 다르면 같은 지표로 비교하지 않는다.
- 발견은 조회한 값으로 만들고, 요약문에서 다시 추출하지 않는다.
- 근거가 부족하면 발견 개수를 억지로 채우지 않는다.
- 크기를 계산할 수 없는 발견에는 억지 순위를 만들지 않는다.

(다음은 기존 항목과 같은 의미라 새로 적지 않았다.
"표본이 작으면 파생 비율 비교를 만들지 않는다" → [신뢰성/표시]의 "표본이 적으면 할 수 있는 것만 한다. 최소 표본 기준을 두고 미달이면 판정을 보류한다"와 같다.
"관측기간이 아직 끝나지 않은 코호트는 완전 관측된 성과처럼 비교하지 않는다" → [계산/그레인]의 "관측기간이 다른 코호트를 단순 성과 비교하지 않는다"와 같다.
"화면에서 감춘 값은 문서 근거로 다시 사용하지 않는다" → [문장/책임]의 "화면에서 감춘 숫자는 리포트·PDF·아카이브 어디에도 다시 쓰지 않는다"와 같다.
"발견에는 원인을 섞지 않는다" → [문장/책임]의 "자동 문장과 사람 문장 모두 인과 표현 검사를 적용한다"·"관측 데이터만으로는 인과를 단정하지 않는다"와 같다.)

[오늘 실제로 내린 결정]

- 신뢰 가능한 세그먼트 비교축이 없으면 분해 비율을 발견 근거로 쓰지 않는다.
- 표본 기준 미달 항목은 "못 쓰는 숫자"로 분리한다.
- 연도별 값이 있어도 관측조건이 다르면 성과 비교 문장으로 쓰지 않는다.
- 현재 근거가 부족하면 발견 1개만 확정해도 된다.
- 발견 크기를 계산할 공통 비중이 없으면 격차×비중을 적용하지 않는다.

### 2026-09-11

오늘 제안서 기능(자동/사람 절 조립, HTML/PDF 출력) 작업 중 나온 원칙과 결정을 정리한다. 기존 항목과 의미가 겹치는 것은 새 항목으로 추가하지 않고 이 절 끝에 어느 기존 항목과 같은지 남긴다.

[문서 자동화 원칙]

- 자동/사람을 가르는 기준은 다시 조회하면 같은 결과가 나오는가이다.
- 카드나 근거 파일에 없는 값을 화면에서 새로 만들어 채우지 않는다.
- 값이 없으면 "미확인" 또는 todo로 정직하게 남긴다.
- 기존 문장검사 로직이 있으면 새로 만들지 않고 재사용한다.
- 자동 절은 읽기 전용으로 두고 사람 절만 편집 가능하게 한다.
- 후보는 자동으로 보여줄 수 있지만 선택과 다음 행동은 사람이 결정한다.
- 기능을 먼저 만들고 최종 메뉴 위치는 나중에 정할 수 있다.

[문서 구조 원칙]

- 본문 순서는 의미가 있으므로 임의로 바꾸지 않는다.
- "하지 말 것 → 다시 할 것 → 할 것" 순서를 유지한다.
- 한 장 요약은 발견·제안·불확실·근거 4항목을 넘기지 않는다.
- 요약에 배경·목적·방법론을 추가하지 않는다.
- HTML/PDF는 화면과 동일한 사실을 사용해야 한다.

(다음은 기존 항목과 같은 의미라 새로 적지 않았다.
"문서는 자동으로 쓰는 절과 사람이 쓰는 절을 나눈다"·"판단이 필요한 절을 자동화하면 책임의 주체가 사라진다" → [문장/책임]의 "결과가 틀렸을 때 사람이 책임지는 문장은 사람이 쓰고, 사실만 틀릴 수 있는 장은 자동으로 쓴다"와 같다.
"화면에서 지킨 미확인 상태는 HTML/PDF에서도 그대로 유지한다" → 위 "HTML/PDF는 화면과 동일한 사실을 사용해야 한다"와 [문장/책임]의 "화면에서 감춘 숫자는 리포트·PDF·아카이브 어디에도 다시 쓰지 않는다"를 합친 것과 같다.
"미확인 값을 그럴듯하게 채우는 것보다 비워 두는 것이 낫다" → 위 "값이 없으면 '미확인' 또는 todo로 정직하게 남긴다"와 같다.)

[오늘 실제로 내린 결정]

- 제안서의 자동 절은 한 장 요약·하지 말 것·다시 할 것·할 것·부록으로 구분한다.
- 사람 절은 "이 제안이 틀린다면"과 "적용"으로 둔다.
- 적용 절의 후보는 자동 표시하지만 실제 선택과 작성은 사람이 한다.
- 사람이 쓰지 않은 human 절은 자동 문장으로 채우지 않는다.
- HTML과 PDF에서 카드에 없는 값을 생성하지 않는다.

### 2026-09-12

- 채울 데이터 함수를 댈 수 없는 절은 만들지 않는다 — 빈 절 대신 절 자체를 없앤다.
- 신뢰할 수 없는 비교는 후보로 만들지 않되, 공식 기준이 없는 격차를 임의로 작다고 판단해 기각하지 않는다.
- 그림 하나에는 그것이 증명하는 문장 하나가 있어야 한다 — 대응 문장이 없는 그림은 장식이므로 넣지 않는다.
- 요청 문장에 결정 동사가 없으면 그것은 보고이지 제안이 아니다 — 최종 문서는 읽는 사람이 승인/조건부 승인/보류 중 하나를 판단할 수 있어야 한다.
- 내부 키와 화면에 보이는 표시 문자열은 분리하고, 문서에 쓰이는 말은 한 곳에서만 관리한다.

### 2026-09-12 — 7주차 함정 재점검

7주차에 정리했던 네 가지 분석 함정(그레인 오류·가드레일 없는 성공 판정·실험 설계
함정·생존편향/관측편향)을 지금 나온 결정 문서 하나에 실제로 대입해 다시 점검한
결과다. 새로 계산하거나 근거를 꾸미지 않고, 이미 있는 코드·문서에서 확인되는
사실만 적는다.

- 하나로 세는 두 대상의 관계가 지금은 1:1이라도, 그 사실이 코드 어딘가에 전제로만
  깔려 있고 실제로 강제되지 않으면 관계가 바뀌는 순간 조용히 틀어질 수 있다 —
  이런 전제는 계산 결과를 보여줄 때 "지금은 이렇다"고 같이 밝혀 둬야 한다.
- 범위가 좁은 결정(예: 확인 대상 지정, 원칙 승인)에도 판단을 되돌릴 조건과
  조건부로 결정할 수 있는 선택지를 함께 제시해야, "문제없다"는 말만 반복하는
  가드레일 없는 성공 선언이 되지 않는다. 다만 그 조건을 적어야 할 절이 비어
  있으면 가드레일이 형식만 있고 내용은 없는 상태로 나갈 수 있다.
- 실험이 아니라 관찰한 값만으로 만든 결정 문서라도, 인과를 단정하는 표현을
  걸러내고, 실측값과 환산값을 한 문장에 섞지 않고, 관측 기간이 다른 값을
  성과 비교에 쓰지 않고, 근거 없는 효과 수치를 새로 만들지 않으면 실험처럼
  결과를 단정하는 함정은 피할 수 있다.
- 아직 결과가 나올 시점이 되지 않은 대상을 실패로 세지 않고 별도로 표시해
  두었다가, 그 시점이 됐을 때 다시 확인한다는 원칙을 문서에 남겨 두면
  생존편향·관측편향을 줄일 수 있다. 다만 그 "다시 확인"이 실제로 일어나는지는
  문서 한 장만으로는 보장되지 않고 별도 절차로 이어져야 한다.

### 2026-09-12 — 외부 벤치마크가 보장하지 않는 것

- 벤치마크 점수가 낮다고 해서 실제 의사결정자가 그 결과를 채택한다는 보장은
  아니다 — 점수와 채택 여부는 서로 다른 질문이다.
- 점수가 낮다고 모든 지적을 그대로 반영해야 하는 것도 아니다 — 반영 여부는
  점수가 아니라 지적 하나하나를 따로 판단해서 정한다.
- 지적은 실제로 없는 정보인지, 이미 있지만 위치·표현 때문에 못 찾은 것인지,
  지금 결정 범위 밖의 요구인지로 구분한 뒤에만 반영 여부를 정한다.
- 문서에 없는 비용·효과·담당자·목표값은 점수를 올리기 위해 새로 만들지 않는다
  — 없으면 없다고 남긴다.
- 수정할지 말지는 점수가 오르는가가 아니라, 그 정보가 지금 요청한 결정을
  내리는 데 실제로 필요한가로 판단한다.

### 2026-09-12 — 누적 판단기준 결산

A. 겹치는 원칙

- 표본이 부족한 값은 근거로 쓰지 않고 판정을 보류한다는 원칙이, 일반 규칙과
  세그먼트별 적용 두 군데에서 반복된다.
- 관측 기간이 다른 대상을 성과 비교에 쓰지 않는다는 원칙이, 서로 다른 날짜에
  표현만 바꿔 반복된다.
- 관측 데이터만으로 인과를 단정하지 않는다는 원칙이, 일반 규칙과 함정 재점검
  기록 두 곳에서 반복된다.

B. 후속조치 후보

| 후보 | 언제 적용하는가 | 실제 행동 |
|---|---|---|
| 신뢰조건 재점검 | 새로운 데이터·세그먼트·비교 축이 생겼을 때 | 표본·신뢰 조건을 다시 확인하고, 조건을 못 채우면 판정을 보류한다 |
| 관측기간 도래 시 재평가 | 보류해 둔 항목의 관측 기간이 실제로 끝났을 때 | 그 시점에 다시 확인해 성과 비교에 쓸 수 있는지 재평가한다 |
| 승인 전 결정요건 확인 | 결재·승인을 요청하는 문서를 제출하기 직전 | 결정 동사·조건부 선택지·철회 기준이 실제로 채워졌는지 확인한 뒤에만 제출한다 |

### 2026-09-12 — 벤치마크 최저 항목 처리

[평가 결과]

- 최저점 동점: 실행 가능성과 효과 0/20, 리스크와 검증 0/10
- 이번에 선택한 항목: 실행 가능성과 효과 0/20

[선택 이유]

- 배점이 더 크고
- 실제 승인 가능성을 막은 핵심 지적과 직접 연결됐기 때문

[A/B/C 처리]

- 실행방법의 최소 정의 부재: A → 확인 대상/목적/시점/후속조치 반영
- 2027년 확인 대상 선정 이유가 문서에 보이지 않음: B → 기존 카드1 근거를 제안서에 1회 노출
- 담당자 실명/상세 업무분장/구체 일정표/구체 예산/임의 KPI: C → 이번 승인 범위 밖 상세 실행계획으로 유지
- 확인 주체 및 추가 시스템/외부비용/추가 인력 여부: 실제 조직 확인이 필요한 조건부 확인사항으로 유지

[반영하지 않은 것]

- 비용 금액
- 효과 수치
- 담당자 실명
- 임의 KPI 목표
- 근거 없는 완료율 목표

이 값들은 점수를 올리기 위해 만들지 않았다.

[결론]

- 최저 항목 지적을 그대로 전부 수용하지 않고, 실제 의사결정에 필요한 최소 정보만 반영했다.
- 외부 독자 판정은 최종적으로 조건부 채택까지 개선됐다.

### 2026-09-12 — 판단기준 누적 이력 검증

이 소절은 "원래 판단 회차"로 세지 않는 메타 기록이다 — 새 판단 원칙을 추가하는
것이 아니라, 지금까지 쌓인 판단 회차 수를 있는 그대로 확인해 남긴다.

- 현재 저장소에서 원문 근거로 확인 가능한 독립 판단 기록은 5회다.
- 확인 가능한 기록: 1) 2026-09-04, 2) 현재 프로젝트 결정, 3) 2026-09-09,
  4) 2026-09-11, 5) 2026-09-12.
- 저장소 최초 커밋은 2026-09-04이며, 그 이전 판단기록의 원문이나 Git 이력이
  존재하지 않는다.
- 따라서 총 8회 누적 요건 중 부족한 3회는 근거 없이 소급 작성하지 않는다.
- 향후에는 각 작업일의 판단기준을 당일 저장해 누적 이력을 유지한다.
