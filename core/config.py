# -*- coding: utf-8 -*-
"""앱 설정.

**도메인을 바꿀 때 가장 먼저 고치는 곳이 이 파일이다.**

다만 이 파일만 고치면 끝나지 않는다. 컬럼명은 metrics.py 안에도 박혀 있어서,
config를 다 바꾸고 앱을 켜면 거기서 깨진다. **깨지는 것이 정상이다.**
순서는 DESIGN.md §4-6에 있다.

    1. 여기 FUNNEL_STEPS 를 내 단계로 바꾼다
    2. load.py 가 내 파일을 읽게 한다
    3. 앱을 켠다 -> 깨진다
    4. metrics.py 의 컬럼명을 하나씩 맞춘다
    5. validate.py 에서 내 데이터에 맞지 않는 검증을 뺀다

────────────────────────────────────────────────────────────────────
7주차 토요일에 만든 「명세 5행」이 여기로 들어온다.

  명세 1행  데이터                 ->  DATASET · PERIOD · TABLES
  명세 2행  퍼널 단계              ->  FUNNEL_STEPS · FUNNEL_LABELS
  명세 3행  주지표 + 가드레일       ->  metrics.py 에서 계산
  명세 4행  임계값 · 최소 표본      ->  THRESHOLDS · MIN_SAMPLE
  명세 5행  유지 / 이탈의 정의      ->  RETENTION_STEPS
────────────────────────────────────────────────────────────────────

지금 값은 **통신사 예시**다. ★ 표시가 붙은 것을 내 것으로 바꾼다.
자기 데이터가 없으면 그대로 두고 진행해도 된다.
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DATA_DIR = ROOT / "data"          # 데이터 파일
RUNS_DIR = ROOT / "runs"          # 실행 이력
ASSETS_DIR = ROOT / "assets"      # 폰트 등

# ── 데이터 ────────────────────────────────────────────────────────
# ★ Day1 — 명세 1행
DATASET = "내부회계관리제도 운영 및 개선관리"
PERIOD = ("2025-01-01", "2026-12-31")

# ★ 내 테이블 목록. 하나뿐이어도 된다. 파일명(확장자 제외)과 같아야 한다.
TABLES = [
    "ic_process_master", "ic_control_master", "ic_design_assessment",
    "ic_operating_assessment", "ic_deficiency", "ic_remediation",
]

# ── 퍼널 ──────────────────────────────────────────────────────────
# ★ Day1 — 명세 2행. 순서가 곧 단계 순서다.
#   단계 수는 도메인이 정한다. 6개일 이유가 없다 — 3개여도 8개여도 된다.
#   퍼널이 아니면 퍼널이라고 부르지 않는다: 앞 단계를 반드시 거치는가?
FUNNEL_STEPS = ["미비점발생", "개선조치착수", "개선조치완료"]
FUNNEL_LABELS = {
    "미비점발생": "미비점 발생",
    "개선조치착수": "개선조치 착수",
    "개선조치완료": "개선조치 완료",
}

# ── 유지 퍼널 ─────────────────────────────────────────────────────
# ★ Day2 — 명세 5행. 7주차 토요일에 내가 정한 유지·이탈의 정의를 옮긴다.
#
#   획득 퍼널은 단계가 주어졌지만 유지는 순서가 없다. 내가 정의해야 한다.
#   각 단계는 (이름, 설명) 이고, 계산은 metrics.retention_funnel() 에서 한다.
#
#   예시 - 통신사:
#     [("가입", "전체 고객"),
#      ("활성", "마지막 달에 사용 기록이 있음"),
#      ("이탈", "is_churned = True")]
#
#   이탈 개념이 없는 도메인이면 재이용률·반복 요청률로 바꾼다. (DESIGN.md §4-4)
RETENTION_STEPS: list[tuple[str, str]] = []

# ── 비즈니스 파라미터 ─────────────────────────────────────────────
# 전부 **가정값**이다. 실측 테이블에서 유도하지 않는다.
# 광고비(ad_spend)는 채널 효율(ROAS·CPC) 전용이고, 총액을 가입자 수로 나눠
# CAC를 만들지 않는다 — 광고비는 개인 단위로 추적되지 않아 가입과 이을 수 없다.
# 리포트에 이 값이 들어가는 문장에는 "가정값 기반"을 붙인다. -> DESIGN.md §4-3
#
# ★ 비용 개념이 없는 도메인이면 투입 공수(인시)로 바꿔도 된다. 금액이 아니어도
#   "무엇을 얼마나 썼는가"면 된다.
CHANNEL_CAC = {
    "검색": 140_000, "디스플레이": 180_000, "제휴": 100_000,
    "추천": 30_000, "오프라인": 250_000,
}
CONTRIBUTION_MARGIN = 0.30      # 공헌이익률
LTV_MONTHS = 36                 # 평균 유지기간

# ── 지표 임계값 ───────────────────────────────────────────────────
# ★ Day1 — 명세 4행. **비워 두었다. 내가 정한다.**
#
#   남이 정해줄 수 없는 유일한 값이다. 업계 벤치마크·직전 기간·목표치 중
#   무엇을 근거로 삼았는지 위키 지표 정의서에 한 줄로 적는다.
#   근거 없는 임계값은 남의 것이다.
#
#   이름이 여기 없는 지표는 "ok"가 아니라 "참고지표"로 뜬다(임계값 미정과
#   정상 범위는 다른 말이다 — core/metrics.py status_of() 참고).
#
#   형식:  "지표이름": {"경고": 값, "위험": 값}
#   예시 - 통신사:
#     "전환율": {"경고": 3.0, "위험": 2.5},      # % 미만이면
#     "이탈률": {"경고": 18.0, "위험": 22.0},    # % 초과면
#
#   ★ 공식 확정: metric-001 미해소 미비점 비율(my-wiki-04 analysis-005
#     B안, 낮을수록 좋음). metric-002~004는 아직 공식 임계값이 없어
#     임의로 만들지 않았다 — "참고지표"로 표시된다(CLAUDE.md 참고).
THRESHOLDS: dict[str, dict[str, float]] = {
    "미해소 미비점 비율": {"경고": 63.64, "위험": 68.18},
}

# ★ 이보다 표본이 적으면 "표본 부족"으로 보고 지표를 계산하지 않는다.
#   내 상황에 맞게 낮춰도 된다. **다만 없애지는 않는다.** -> DESIGN.md §4-5
MIN_SAMPLE = 20

# ── 상태 색 ───────────────────────────────────────────────────────
# 의미가 고정된 규칙이다. **도메인이 바뀌어도 바꾸지 않는다.**
COLORS = {
    "ok": "#10b981",       # 통과 / 정상
    "warn": "#f59e0b",     # 경고 / 표본 부족 / 가드레일 악화
    "block": "#f43f5e",    # 차단 / 검증 실패 / 실험 무효
    "none": "#64748b",     # 데이터 없음 / 효과 없음
}
# 브랜드 색은 자유롭게 바꿔도 된다.
BRAND = {
    "primary": "#4f46e5",
    "ink": "#0f172a",
    "muted": "#64748b",
    "line": "#e2e8f0",
    "bg": "#f8fafc",
    "surface": "#ffffff",
}

# ── 리포트 ────────────────────────────────────────────────────────
# 8장 중 3장은 사람이 쓴다. **이 구분은 바꾸지 않는다.**
# 해석과 제안을 자동화하는 순간 책임이 사라진다.
REPORT_SECTIONS = [
    ("1. 요약", "auto"),
    ("2. 배경", "human"),
    ("3. 방법", "auto"),
    ("4. 결과", "auto"),
    ("5. 실험", "auto"),
    ("6. 해석", "human"),
    ("7. 한계", "auto"),
    ("8. 제안", "human"),
]

# 발송은 초안까지만. 실제로 전송하지 않는다.
EMAIL_ENABLED = False
EMAIL_TO_EXAMPLE = "team-growth@example.com"

# ── 제안서 표시 문자열 ────────────────────────────────────────────
# report/proposal.py·pages/5_제안서.py가 화면·인쇄에 실제로 보여주는
# 문자열만 모은다. **내부 데이터 키(분류_목록·SECTION_SPECS의 "키" 등,
# 제안카드.md의 "분류:" 값)는 여기서 바꾸지 않는다** — 계산·조회는 그
# 내부 키로 그대로 하고, 표시할 때만 이 사전을 거친다(계산 계층과 표시
# 계층을 분리한다).
PROPOSAL_WORDS = {
    # 절 제목/질문 — 내부 키(summary/status/scale_trend/proposal/risk/request)는
    # report/proposal.py의 SECTION_SPECS 순서·kind 그대로 유지한다.
    "sections": {
        "summary": {
            "title": "핵심 요약",
            "question": "아래 절 전체를 읽지 않아도 핵심을 판단할 수 있는가",
        },
        "status": {
            "title": "현황 및 문제점",
            "question": "현재 어떤 주제가 발견됐고 어떤 현황인가",
        },
        "scale_trend": {
            "title": "규모 및 추세",
            "question": "연간 환산 규모와 최근 추세는 무엇인가",
        },
        "proposal": {
            "title": "제안 내용",
            "question": "어떤 행동을 제안하는가",
        },
        "risk": {
            "title": "판단의 한계 및 철회 기준",
            "question": "어떤 조건이면 판단을 되돌려야 하는가",
        },
        "request": {
            "title": "결정 요청",
            "question": "승인/조건부 승인/보류 중 무엇을 결정해야 하는가",
        },
    },
    # 제안카드.md·분류_목록의 내부 키("하지 말 것"/"다시 할 것"/"할 것")는
    # 그대로 두고, 화면/문서에 보일 때만 이 명사형 표현으로 바꾼다.
    "classification": {
        "하지 말 것": "제외",
        "다시 할 것": "재평가",
        "할 것": "실행",
    },
    # 판정/상태 라벨 — core/metrics.py의 판정 로직·반환값 자체는 바뀌지 않는다.
    "status_terms": {
        "신뢰 가능": "표본 기준 충족",
        "관측기간 주의": "비교 판단 보류 사유",
        "규모 산정 불가": "규모 근거 부족",
        "기각사유": "우선순위 제외 사유",
    },
    # 절 kind 배지
    "kind": {
        "auto": "데이터 산출",
        "human": "검토 의견",
    },
    # 아직 확정되지 않은 값의 표시 — 원본 값("미확인" 등)은 바꾸지 않고
    # 표시할 때만 이 문구로 바꾼다.
    "pending": {
        "미확인": "현재 근거 없음",
        "작성 필요": "검토 의견 없음(입력 필요)",
    },
    # 내부 컬럼명/코드 용어 → 표시용 한국어(값 변경이 아니라 표시 변환).
    "display_terms": {
        "target_date": "개선조치 목표일",
        "identified_date": "미비점 발생일",
        "completion_date": "개선조치 완료일",
        "action_status": "개선조치 상태",
        "deficiency_id": "미비점 식별자",
        "control_id": "통제 식별자",
        "config.PERIOD": "분석 기간",
        "config.THRESHOLDS": "판정 기준",
        "config.MIN_SAMPLE": "최소 표본 기준",
        "funnel(": "단계별 집계(",
        "monthly(": "월별 집계(",
        "topic_evidence": "근거 조회",
        "proposal_topics": "제안 주제 조회",
    },
}
