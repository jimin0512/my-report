# my-report — 신뢰성 판정 규칙

이 문서는 2026-09-04에 처음 만들어졌다(이전에는 my-report에 CLAUDE.md가 없었다). "신뢰할 수 없는 숫자는 계산하지도, 표시하지도 않는다"는 원칙을 코드와 별도로 문서화해 둔다. 도메인 자체 설명(테이블·지표 정의 등)은 `../my-wiki-04`가 원본이며, 여기서는 앱 전체가 지켜야 할 신뢰성 판정 규칙만 적는다.

## 개요

- 목적: 내부회계관리제도(설계평가·운영평가·미비점·개선조치)의 데이터를 넣으면 검증 → 계산 → 대시보드 → 리포트까지 이어지는 흐름을 보여준다.
- 데이터셋: `config.DATASET`("내부회계관리제도 운영 및 개선관리"), 분석 기간 `config.PERIOD`(2025-01-01~2026-12-31).
- 테이블 6종(`config.TABLES`): `ic_process_master`(프로세스)·`ic_control_master`(통제, `key_control` 컬럼으로 Key/Non-Key 구분)·`ic_design_assessment`/`ic_operating_assessment`(연 1회 평가)·`ic_deficiency`(미비점)·`ic_remediation`(개선조치).
- KPI 4종(metric-001~004)의 정의 원본은 `../my-wiki-04/06_metrics/*.yaml`이다. 이 앱은 그 정의를 코드로 옮겨 계산만 하고, 새로 정의하지 않는다.
- `../my-wiki-04`는 읽기 전용이다. 앱이 그 폴더의 파일을 쓰기 대상으로 열지 않는다.
- `../auto-report`는 별개 앱이다. 이 프로젝트의 어떤 작업도 그 폴더를 수정하지 않는다.

## 계산 그레인

| 지표/표 | 그레인 | 비고 |
|---|---|---|
| Track B(`funnel`) | 미비점 1건(`deficiency_id`) | 개선조치는 미비점과 1:1(현재 데이터) |
| 운영 적정률(`kpis`) | 평가 레코드(`assessment_id`) | 현재 데이터는 통제당 연 1회 평가라 결과적으로 `control_id` 수와 같다 — 정의상 그레인은 평가 레코드다 |
| 반복 미비점 통제 비율 | 통제(`control_id`) | 서로 다른 연도 2개 이상에서 미비점 발생 여부 |
| 개선 후 재발(`retention_funnel`) | 통제(`control_id`) | 미비점 행 수가 아니라 통제 집합 연산으로 판정 |
| monthly | 미비점 발생=`identified_date`, 개선완료=`completion_date`(`action_status`='완료'인 것만) | 발생·완료가 없는 달도 0으로 채운다 |

## 신뢰성 판정 규칙 (2026-09-04 확정)

- 최소 표본은 `config.MIN_SAMPLE = 20`건이다. n < 20이면 판정 보류.
- 못 믿는 조건은 계산보다 먼저 판정한다 — `core/metrics.py`의 `trust_check()`가 이 판정 지점이다.
- 못 믿으면 지표(비율) 계산 자체를 하지 않는다. "계산해 놓고 화면에서 숨기는" 것이 아니라 "계산하지 않는" 구조여야 한다.
- 계산 후 숨기기 금지. 판단이 항상 나눗셈(비율 계산)보다 먼저 실행돼야 한다.
- 감춘 항목에는 지표 값·증감·비교차이를 다시 남기지 않는다. 단, 원시 건수(모집단·유지·재발 같은 절대 건수)는 사실값이므로 남길 수 있다.
- 사유에는 항상 실제 조건값과 기준값을 함께 적는다. 예: "표본 15건 (최소 20건)". "소표본 주의"처럼 모호한 문구만 쓰지 않는다.
- 관측기간이 덜 찬 코호트(예: target_date가 아직 오지 않은 개선조치)는 값 자체는 계산해 보여주되, 전년 대비 성과가 좋아졌다/나빠졌다는 비교 판정에는 쓰지 않는다.
- 전후 비교는 인과를 주장하지 않는다.
- 주지표·비교 결과보다 신뢰성 판정을 먼저 수행한다.
- 화면(대시보드)·리포트 본문·PDF·아카이브 metadata 전부에 같은 규칙을 적용한다 — 화면에서 감춘 숫자가 리포트나 아카이브에 다시 나타나면 안 된다.

## 현재 판정 보류 상태 (2026-09-04 기준, `core/metrics.py trust_check()` 결과)

| 항목 | 표본 | 상태 | 판정 |
|---|---:|---|---|
| 개선 후 재발 분석 | 15건 (최소 20건) | 판정 보류 | 유지율·재발률 비율 미표시, 원시 건수(모집단 15·유지 10·재발 5)만 표시 |
| 재고 프로세스 운영 적정률(2026) | 7건 (최소 20건) | 판정 보류 | 전체 평균(80.00%) 대비 비교 판정 보류, 원시 건수(5/7)만 표시 |
| Key통제 개선완료율(2026) | 7건 (최소 20건) | 판정 보류 | Non-Key 대비 비교 판정 보류, 원시 건수(2/7)만 표시 |
| 프로세스별(funnel_by) 전환율 | 최대 12건(재고) | 판정 보류(전체) | 7개 프로세스 모두 최소표본 미달로 프로세스별 전환율 비교 전체 보류, 발생 건수만 표시 |
| 2026년 개선조치 관측기간 | 22건 (최소 20건 충족) | 관측기간 경고(값은 표시) | 22건 중 12건(54.55%)이 target_date 2027년 — 값(40.91%)은 계산하되 전년 대비 성과 판정에는 쓰지 않음 |

## KPI 상태 판정 규칙 (2026-09-04 확정)

- 임계값이 `config.THRESHOLDS`에 정의된 KPI만 정상/경고/위험을 판정한다.
- 임계값이 아직 없는 KPI는 "참고지표"로 표시한다. "참고지표"는 정상(ok)과 같은 색을 쓰지 않는다 — `core/metrics.py status_of()`가 이름을 못 찾으면 "ok"가 아니라 "none"을 돌려주고, 화면은 이를 "○ 참고지표"(중립색)로 그린다.
- 참고지표는 신뢰성 판정 보류("block", "✕ 판정 보류")와 다른 개념이다: 참고지표는 값은 믿을 수 있지만 아직 공식 정상/경고/위험 기준이 없는 상태이고, 판정 보류는 표본·관측기간 조건 미충족으로 비율 자체를 계산하지 않은 상태다. 두 상태는 코드에서도(`status_of()` vs `trust_check()`) 화면에서도(색·문구) 서로 섞지 않는다.
- 임계값을 임의로 만들지 않는다. 현재 공식 확정된 것은 metric-001(미해소 미비점 비율, my-wiki-04 analysis-005 B안)뿐이다. metric-002~004(운영 적정률·반복 미비점 통제 비율·개선완료율)는 공식 임계값이 확정되기 전까지 참고지표로 남는다.

## 유지·재발은 퍼널이 아니다

"판정 모집단 15 → 유지 10 / 재발 5"는 순차적으로 거치는 단계가 아니라, 판정 모집단을 유지·재발 두 결과로 나누는 상호배타적 분류다("유지"를 거쳐야 "재발"이 되는 구조가 아니다). 그래서 화면 명칭은 "유지·재발 구성"이고, 퍼널 차트나 "전환율" 표현을 쓰지 않는다. `config.RETENTION_STEPS`는 순차 단계가 있는 도메인을 위한 자리라 이 분석에는 쓰지 않는다(빈 리스트로 둔다 — 억지로 채우지 않는다). 함수명 `retention_funnel()`은 다른 코드 호환성 때문에 그대로 둔다 — 사용자 노출 명칭만 정확히 한다.

## KPI delta 정책

전년 대비 delta는 관측기간 이슈가 없는 지표에만 보여준다.

- 운영 적정률: delta 표시(2025 81.67% → 2026 80.00%, 두 연도 모두 완전히 관측됨).
- 미해소 미비점 비율·개선완료율: delta 미표시. metric-001·004.yaml의 "유효구간" 절에 "계산 시점에 따라 달라질 수 있다"고 이미 적혀 있고, 2026년 개선조치 22건 중 12건이 target_date 2027년이라 2026년 값이 아직 완료할 기간이 남은 상태에서 관측됐다 — 전년 대비 비교 판정에 쓰지 않는다.
- 반복 미비점 통제 비율: delta 미표시. 연도별 값이 없는 결합 스냅샷이라 비교할 "전년 값" 자체가 없다.

## 제안서(report/proposal.py) 원칙 (2026-09-11 확정)

- `report/proposal.py`는 `report/sections.py`와 같은 자동/사람 분리 원칙을 따른다.
- 현재 구분 — 자동: 한 장 요약·하지 말 것·다시 할 것·할 것·부록(근거 상세). 사람: 이 제안이 틀린다면·적용.
- 자동 절에는 제안카드.md에 없는 값을 새로 만들어 넣지 않는다. 값이 없거나 확인되지 않았으면 임의로 채우지 않고 "미확인" 또는 todo 상태로 남긴다 — 화면·HTML·PDF 전부에 같은 규칙을 적용한다.
- 제안서 문장검사는 새 로직을 만들지 않고 `report.sections.check_phrasing()`과 기존 `BANNED` 목록을 그대로 재사용한다.
- "적용" 절의 후보 문장(판단기준.md에서 추출)은 자동으로 보여줄 수 있지만, 무엇을 선택하고 다음에 무엇을 볼지는 사람이 결정한다.

## 갱신 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-04 | 최초 작성. 신뢰성 판정 규칙 확정, `trust_check()`에 `trusted`/`reason`/`actual`/`threshold` 필드 추가 및 계산-전-차단 구조 반영 |
| 2026-09-04 | KPI 상태 판정 규칙 추가. metric-001에만 공식 임계값(경고 63.64%·위험 68.18%) 등록, 나머지 3개 KPI는 "참고지표"로 표시하도록 `status_of()`가 임계값 미정 시 "none"을 돌려주게 수정(기존에는 "ok"를 돌려줘 전부 정상으로 보였다) |
| 2026-09-04 | 전수 점검 후 보완: 개요·계산 그레인 표 추가, "유지 퍼널" 화면 명칭을 "유지·재발 구성"으로 바꾸고 퍼널 차트·전환율 표현 제거(상호배타적 결과 분류이지 순차 단계가 아니다), `kpis()`에 `year` 파라미터를 추가해 운영 적정률에만 전년 대비 delta 표시, Track B에 단계별 표(ProgressColumn) 추가. 이 점검에서 `runs/`가 아직 비어 있어(실제 게이트 통과 기록 없음) 과거 통과 기록은 만들어 넣지 않았고, Git 저장소도 이번에 처음 초기화했다(과거 커밋으로 위장하지 않음, baseline 커밋으로 남긴다) |
| 2026-09-11 | 제안서(report/proposal.py) 원칙 추가 — 자동/사람 분리, 자동 절의 값 임의 생성 금지·"미확인"/todo 유지(화면·HTML·PDF 공통), 문장검사 재사용, "적용" 후보와 사람 선택의 구분을 기록 |
